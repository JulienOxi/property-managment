{% extends 'base.html.twig' %}

{% block title %}PropertyRent{% endblock %}

{% block body %}
    <h1>PropertyRent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ property_rent.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ property_rent.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ property_rent.type }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ property_rent.price }}</td>
            </tr>
            <tr>
                <th>MonthlyPrice</th>
                <td>{{ property_rent.monthlyPrice }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ property_rent.CreatedAt ? property_rent.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndedAt</th>
                <td>{{ property_rent.endedAt ? property_rent.endedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_property_rent_index') }}">back to list</a>

    <a href="{{ path('app_property_rent_edit', {'id': property_rent.id}) }}">edit</a>

    {{ include('property_rent/_delete_form.html.twig') }}
{% endblock %}
