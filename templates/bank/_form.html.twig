{{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

<div class="md:grid md:grid-cols-3 md:gap-4">

    <div class="">
        {{ form_label(form.name, 'Nom de la banque', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.name, {'attr': {'class': 'input-base'}}) }}
    </div>  

    <div class="">
        {{ form_label(form.email, 'Email', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.email, {'attr': {'class': 'input-base'}}) }}
    </div>

    <div class="">
        {{ form_label(form.website, 'Site web', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.website, {'attr': {'class': 'input-base'}}) }}
    </div>  

    <div>
        {{ form_label(form.bic, 'Bic', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.bic, {'attr': {'class': 'input-base'}}) }}
    </div>
    <div>
        {{ form_label(form.iban, 'Compte Iban', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.iban, {'attr': {'class': 'input-base'}}) }}
    </div>    
    <div>
        {{ form_label(form.clearingNumber, 'N° de clearing', {'label_attr': {'class': 'block text-sm font-medium text-gray-700'}}) }}
        {{ form_widget(form.clearingNumber, {'attr': {'class': 'input-base'}}) }}
    </div>

</div>

<div>
    <button class="mt-4 py-2 px-4 bg-blue-600 text-white font-bold rounded hover:bg-blue-700">
        {{ button_label|default('Enregistrer') }}
    </button>
    <button onclick="history.back()" class="mt-4 py-2 px-4 bg-red-600 text-white font-bold rounded hover:bg-red-700">Annuler</button>
</div>

{{ form_end(form) }}