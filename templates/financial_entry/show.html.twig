{% extends 'base.html.twig' %}

{% block title %}FinancialEntry{% endblock %}

{% block body %}
    <h1>FinancialEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ financial_entry.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ financial_entry.type }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ financial_entry.category }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ financial_entry.amount }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ financial_entry.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ financial_entry.createdAt ? financial_entry.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsPaid</th>
                <td>{{ financial_entry.isPaid ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>PaidAt</th>
                <td>{{ financial_entry.paidAt ? financial_entry.paidAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_financial_entry_index') }}">back to list</a>

    <a href="{{ path('app_financial_entry_edit', {'id': financial_entry.id}) }}">edit</a>

    {{ include('financial_entry/_delete_form.html.twig') }}
{% endblock %}
